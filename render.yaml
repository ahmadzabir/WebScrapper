services:
  - type: web
    name: web-scraper
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run python-scraper.py --server.port=$PORT --server.address=0.0.0.0
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0

